{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "concurrently --kill-others \"yarn watch\" \"nodemon .\""
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "tslint": "^6.1.3",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "tsc": "^2.0.4",
    "yarn": "^1.22.22"
  }
}
